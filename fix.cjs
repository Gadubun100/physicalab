const fs = require('fs'); const c = fs.readFileSync('src/App.jsx', 'utf8'); const idx = c.indexOf('PracticeProblems topic='); console.log('PracticeProblems usage at:', idx); if(idx > -1) console.log('Context:', c.substring(idx-50, idx+100));
